http:
  routers:
    pihole:
      rule: "Host(`pihole.stunthound.com`)"
      entryPoints:
        - websecure
      service: pihole
      tls:
        certResolver: cloudflare

  services:
    pihole:
      loadBalancer:
        servers:
          - url: "http://10.25.1.2:80"